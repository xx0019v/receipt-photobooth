# Core (all platforms)
fastapi>=0.115
uvicorn[standard]>=0.30
pillow>=10.3
qrcode>=7.4
python-multipart>=0.0.9   # multipart upload of the canonical print artifact
httpx>=0.27           # TestClient transport for tests
pytest>=8
